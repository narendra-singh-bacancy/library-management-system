apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  type: NodePort
  selector:
    app: orders
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30011  # Choose a port between 30000–32767
